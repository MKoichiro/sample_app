<!-- CSRF保護のためのメタタグを挿入します。
これにより、フォームからのリクエストが正当なものであることが保証されます。 -->
<%= csrf_meta_tags %>

<!-- コンテンツセキュリティポリシー(CSP)のメタタグを挿入します。
これは、クロスサイトスクリプティング攻撃などのセキュリティ脅威から保護するためのものです。 -->
<%= csp_meta_tag %>

<!-- アプリケーションのスタイルシートへのリンクを挿入します。
"data-turbo-track"オプションにより、
ページが再読み込みされたときにスタイルシートが更新されるかどうかをTurboが追跡します。 -->
<%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

<!-- JavaScriptのインポートマップを挿入します。
これにより、モジュールのインポートが簡単になり、JavaScriptの管理が容易になります。 -->
<%= javascript_importmap_tags %>